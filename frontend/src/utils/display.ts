import type{RiskLevel,ConnectionState}from'@/types';export function pct(v:number){return `${Math.round(v*100)}%`}export function formatCountdown(v:number){return v<1?'now':`${v.toFixed(0)} min`}export function formatSimClock(min:number){const m=((Math.round(min)%1440)+1440)%1440;return `${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`}export function formatClock(min:number){return formatSimClock(min)}export function riskColor(r:RiskLevel|string){return r==='emergency'||r==='critical'?'#ef4b4b':r==='high'?'#e58b3a':r==='moderate'?'#d9a441':r==='low'?'#52c878':'#61b7d8'}export function riskLabel(r:RiskLevel|string){return r.charAt(0).toUpperCase()+r.slice(1)}export function connectionColor(s:ConnectionState){return s==='live'?'#46b8d2':s==='offline'?'#ef4444':'#f59e0b'}export function connectionLabel(s:ConnectionState){return s==='live'?'LIVE':s==='offline'?'OFFLINE':'CONNECTING'}
